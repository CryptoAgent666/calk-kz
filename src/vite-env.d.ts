/// <reference types="vite/client" />

/** Дата сборки 'YYYY-MM-DD' (Алматы), см. define в vite.config.ts. */
declare const __BUILD_DATE__: string;

/** Версия бандла YYYYMMDDHHmmss (как в build-version.json / OTA), 'dev' вне сборки. */
declare const __BUILD_VERSION__: string;
